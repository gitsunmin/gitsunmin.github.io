# How to initialize all changes

Git을 사용하다가 보면, 수정하였던 모든 것을 파기해야할 순간이 옵니다. 물론 저는 VScode를 사용하고 있기 때문에, discard를 해도 되지만, 이미 알고 있는 명령어로도 충분히 Changes에 있는 목록을 초기화할 수 있습니다.

그것은 바로 현재 브랜치로 checkout을 하는 것입니다.
```shell
$ git checkout .
```

이렇게하면, changes에 있는 모든 수정사항이 사라집니다.