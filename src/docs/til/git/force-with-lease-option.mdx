# Force With Lease Option

## Force Push
```shell
git push --force
```
이라는 명령어는 원격 저장소에 강제로 push를 하는 명령어입니다.

## Force With Lease
```shell
git push --force-with-lease
```

이 명령어는 원격 저장소에 강제로 push를 하되, 다른 사람이 push를 한 경우에는 push를 하지 않고 에러를 발생시킵니다.

좀 더 안전하고 강하게 푸시할 수 있습니다.