/**
 * * Munu의 Key로 사용할 id 값을 정의
 */
export const enum MenuId {
  BLOG = 'BLOG',
  SANDBOX = 'SANDBOX',
}

/**
 * * Munu를 클릭 시 이동할 URL을 정의
 */
export const enum MenuURL {
  BLOG = '/',
  SANDBOX = '/sandbox',
}

/**
 * * Munu의 타이틀을 정의
 */
export const enum MenuTitle {
  BLOG = '블로그',
  SANDBOX = '샌드박스',
}
