---
title: 인쇄 템플릿 라이브러리
draft: false
---

## 이 레포의 역할

식봄 물류 현장에서 사용하는 **배송 라벨**과 **거래명세서**를 React 컴포넌트로 구현한 인쇄 전용 라이브러리입니다. 판매자(유통사) 담당자가 상품을 출고할 때 프린터로 출력하는 모든 서류를 이 라이브러리 하나로 생성합니다.

{/* 96개 커밋 중 94개를 직접 작성한 단독 개발 프로젝트입니다. */}

## 템플릿 목록

| 템플릿 | 용지 | 용도 |
|--------|------|------|
| 일반배송 라벨 | 100×100 mm | 일반 식자재 배송 라벨 |
| 저장품 라벨 (비이력·비축산) | 100×100 mm | 저장품 라벨 (비이력 추적, 비축산) |
| 저장품 라벨 (이력·비축산) | 100×100 mm | 저장품 라벨 (이력 추적, 비축산) |
| 저장품 라벨 (이력·축산물) | 100×100 mm | 저장품 라벨 (이력 추적, 축산물) |
| 거래명세서 (일반배송) | A4 | 일반 배송 거래명세서 |
| 거래명세서 (저장품배송) | A4 | 저장품 배송 거래명세서 |

## 기술 스택
> 기술스택은 퇴사 시점 기준으로 작성되었습니다.

| 기술 | 버전 | 역할 |
|------|------|------|
| React | 18.3.1 | 템플릿 렌더링 |
| TypeScript | 5.5.3 | 정적 타입 |
| Vite | 5.4.8 | 번들러 (UMD 빌드 포함) |
| Tailwind CSS | 3.4.14 | 인쇄 레이아웃 스타일 |
| Boronter | git submodule | 인쇄 그리드 렌더링 엔진 |

## 아키텍처

### 배포 방식

TypeScript 소스가 아닌 **UMD 번들**로 빌드되어 사내 CDN에 배포됩니다. PHP 레거시 웹을 포함한 모든 서비스가 CDN을 통해 동일한 버전을 불러옵니다.

```
빌드  → UMD 번들 (.umd.cjs)
배포  → 사내 CDN
사용  → window.Boronter.template(payload)
        window.Boronter.print(payload)
```

### 그리드 기반 레이아웃

각 템플릿은 **mm 단위 행(ROWS)·열(COLS) 배열**로 용지를 격자 분할하고, `area` 맵으로 각 셀에 필드를 배치합니다. 이 방식으로 픽셀 단위 절대 좌표 없이 인쇄 결과를 정밀하게 제어합니다.

```typescript
// 일반배송 라벨 레이아웃 예시
const ROWS = [10, 12, 10, 6, 6, 6, 6, 6, 10, 8, 4]; // mm 단위 행 높이
const COLS = [11, 48, 11, 22];                        // mm 단위 열 너비

const area = [
  ['좌측바코드', '납품처명',   '납품처명',   'QR_CODE'],
  ['좌측바코드', '품명',       '품명',       'QR_CODE'],
  // ... 11행
];
```

### Provider 패턴

각 필드는 데이터를 받아 Boronter 컴포넌트 구조로 변환하는 **Provider 함수**로 정의됩니다. 템플릿은 Provider 함수들을 조합하여 완성됩니다.

```typescript
const 좌측바코드Provider = (값: string): FieldBarcode => ({
  type: 'BARCODE',
  props: {
    value: 값,
    vertical: true,
    format: 'CODE128',
    renderer: 'img',
    style: { width: '70mm', height: 40 },
  },
});
```

### 바코드 생성 로직

배송 라벨의 바코드는 물류 시스템과 1:1로 대응하는 고유 값을 인코딩합니다. 기준일로부터 경과 일수를 계산하고, 주문번호·수량·라벨 차수를 조합하여 CODE128 바코드를 생성합니다.

```typescript
// 기준일 기반 경과 일수 계산
const calculateDays = (rd_date: string): string => {
  const [y, m, d] = [rd_date.slice(0,4), rd_date.slice(4,6), rd_date.slice(6,8)];
  const inputDate = new Date(+y, +m - 1, +d);
  const baseDate  = new Date(1900, 0, 1);
  const diffDays  = Math.ceil((inputDate.getTime() - baseDate.getTime()) / 86400000 + 1);
  return diffDays.toString();
};
```

## 주요 개발 작업

### 저장품 라벨 신규 템플릿 추가

축산물 이력제 의무 표시 규정에 대응하여 저장품 라벨을 세 가지 유형(이력·비축산, 이력·축산물, 비이력·비축산)으로 분리 구현했습니다. 각 유형별로 표시해야 하는 항목(원산지, 이력 번호, 이력 QR 코드 등)이 달라 별도 템플릿으로 관리합니다.

### 바코드 값 개선

물류팀 요청에 따라 바코드 구성 값과 자릿수를 조정했습니다. 바코드 값이 바뀌면 물류 시스템 스캐너와의 연동이 깨지므로, 변경 전 실제 프린터·스캐너로 검증하는 QA 프로세스를 거쳤습니다.

### Playground 개발 도구

`json-edit-react` 라이브러리 기반의 인터랙티브 Playground를 직접 구현했습니다. MOCK 데이터를 실시간으로 편집하면 템플릿 미리보기가 즉시 갱신되어, 백엔드 연동 없이 인쇄 결과를 확인하고 검증할 수 있습니다.

![Playground 개발 도구 스크린샷](/assets/works/sikbom/boronter-template/boronter-playground.webp)

### 한글 도메인 코드 (유비쿼터스 언어)

파일명, 변수명, 함수명을 백엔드 도메인 용어와 동일한 한글로 작성했습니다.

```
src/templates/식봄/일반배송_라벨.ts
src/utils/바코드.ts
src/utils/날짜포맷팅.ts
src/utils/원산지.ts
```

프론트엔드 코드가 비즈니스 용어를 그대로 반영함으로써, 커뮤니케이션 비용이 줄고 도메인 전문가(물류 담당자 등)와의 협업이 원활해졌습니다.

## 성과

- 라이브러리 단일 배포로 기술 스택에 관계없이 모든 서비스에서 동일한 인쇄 결과 보장
- Playground 도구로 QA 팀이 백엔드 없이 템플릿 변경 사항을 즉시 검증 가능
- 한글 도메인 코드로 물류·운영 도메인 전문가와의 커뮤니케이션 비용 감소
